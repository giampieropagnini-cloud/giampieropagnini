# Componente FACOLTATIVO: serve solo per la parte Bluetooth (ricerca delle
# telecamere e telecomando virtuale). Il resto funziona senza installare nulla.
# «CONTROLLA LE TELECAMERE.command» propone da solo questa installazione.
#
# A mano:   python3 -m pip install --user -r requirements-bluetooth.txt
# (su Mac, al primo uso del Bluetooth, il sistema chiederà il permesso:
#  va concesso al Terminale.)
bless>=0.2.6
bleak>=0.21
